const id = 12345;
var emailId = "neelam@gmail.com";
let passwordId = "secret"

/* 
prefer not to use var
because of issue in block scope and functional scope
*/

console.table([id,emailId,passwordId])